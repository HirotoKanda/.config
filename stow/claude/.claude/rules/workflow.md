# Workflow

- To undo work, suggest I use /rewind rather than reverting by generation.
- If two attempts to fix the same issue have failed, stop and say so —
  recommend a fresh session with a better-informed prompt instead of a
  third patch on top of confused context.
